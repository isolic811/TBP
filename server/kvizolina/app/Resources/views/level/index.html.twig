{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="titleCruds">Levels list</h1>
    <ul>
        <li>
            <a class="buttonsNew" href="{{ path('level_new') }}">Create a new level</a>
        </li>
    </ul>
    <table class="manage-poi-list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for level in levels %}
            <tr>
                <td><a href="{{ path('level_show', { 'id': level.id }) }}">{{ level.id }}</a></td>
                <td>{{ level.name }}</td>
                <td>
                    <ul>

                        <li>
                            <a href="{{ path('level_edit', { 'id': level.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
